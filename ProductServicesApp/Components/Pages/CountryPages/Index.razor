@page "/countries"
@using Microsoft.EntityFrameworkCore
@using Microsoft.AspNetCore.Components.QuickGrid
@using ProductServicesApp.Models
@using ProductServicesApp.Data
@implements IAsyncDisposable
@inject IDbContextFactory<ProductServicesApp.Data.ApplicationDbContext> DbFactory

<PageTitle>Country</PageTitle>

<h4>Manage Countries</h4>

<p class="mt-3">
    <a class="btn btn-sm btn-secondary" href="countries/create">Add Country</a>
</p>
<div class="table-responsive-md">
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Name</th>
                <th>Code</th>
                <th>Date Added</th>
                <th>Last Updated</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var country in context.Country)
            {
                <tr>
                    <td>@country.Name</td>
                    <td>@country.ShortCode</td>
                    <td>@country.DateCreated</td>
                    <td>@country.DateUpdated</td>
                    <td>
                        <a class="btn btn-sm btn-secondary" href="@($"countries/edit?id={country.Id}")">Edit</a> |
                        <a class="btn btn-sm btn-secondary" href="@($"countries/details?id={country.Id}")">Details</a> |
                        <a class="btn btn-sm btn-outline-danger" href="@($"countries/delete?id={country.Id}")">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>


@code {
    private ApplicationDbContext context = default!;

    protected override void OnInitialized()
    {
        context = DbFactory.CreateDbContext();
    }

    public async ValueTask DisposeAsync() => await context.DisposeAsync();
}

@* <QuickGrid Class="table" Items="context.Country">
    <PropertyColumn Property="country => country.Name" />
    <PropertyColumn Property="country => country.ShortCode" />
    <PropertyColumn Property="country => country.DateCreated" />
    <PropertyColumn Property="country => country.DateUpdated" />
    <PropertyColumn Property="country => country.IsActive" />

    <TemplateColumn Context="country">
        <a href="@($"countries/edit?id={country.Id}")">Edit</a> |
        <a href="@($"countries/details?id={country.Id}")">Details</a> |
        <a href="@($"countries/delete?id={country.Id}")">Delete</a>
    </TemplateColumn>
</QuickGrid> *@